# 项目宪法（Constitution）

- 文档地位：**项目级最高治理规范**。所有贡献（wiki 页面、分支内容、skill、agent、工具、数据）均须遵守本宪法。
- 版本：v0.1（**草案 / 待校验**）
- 创建：2026-06-17
- 与既有文档关系：本宪法是顶层原则；`CLAUDE.md`、`llm-wiki.md`、`wiki/DESIGN.md` 是其下的操作细则。如有冲突，以宪法为准，并应修订细则使其一致。

> **为什么需要宪法**：本项目是多人协作、版本控制的知识基础设施。本宪法源于真实失败模式（详见各条「理由」），目的是让**任何贡献者（人或 agent）都能按统一原则与格式提交，并被一致地校验**。

---

## 第一编 · 知识完整性（Knowledge Integrity）

### P1 来源真实
**声明**：每个事实性陈述必须可追溯到真实存在的本地文件，或工具查得的官方数据。禁止引用不存在的路径、文件、小节。
**理由**：skill 若引用空路径即成空壳；提案曾差点照抄指向不存在目录的模板。
**校验**：对文中每个引用路径执行 `test -e`；WikiLink 能在库内解析。

### P2 查证优先于记忆
**声明**：卡牌文本、规则条文、牌名译名，必须经工具（`card_search` / `rule_search` / `name_translator`）或本地 `raw/` 原文查得，**禁止凭训练记忆作答**。
**理由**：实测中，季宁（Kinnan）异能不仅原文编造、连领域评审者也凭记忆记错（误加"非人类费用+2"）。唯一可信来源是工具查得的官方文本。
**校验**：涉及具体卡牌/规则的 PR，须在说明中附工具查询输出或本地文件行号。

### P3 可溯源与时效
**声明**：数据须标注来源与时间戳；明确区分"数据驱动结论"与"主观推测"；单一或过期数据须声明可信度边界，**禁止外推为"当前/最新"**。
**理由**：cEDH 仅有 1 份 2026-04 周报；原型占比"~40/20/30/10%"无来源且与该周报维度不符。
**校验**：含 meta/统计结论的页面，每个数字有来源标注；时效性措辞合规。

### P4 禁止编造
**声明**：不编造牌名、规则号、套牌、组合技、数据、能力或"必胜"策略。
**理由**：直接污染所有引用者。
**校验**：评审抽查关键论断的出处。

---

## 第二编 · 架构分层（Layered Architecture）

### P5 分层职责，分支不破坏通用层
**声明**：通用知识库（`wiki/concepts/`、`entities/` 等）回答"**是什么**"；分支层（`wiki/branches/*`）回答"**该分支怎么处理**"。分支层只消费、不修改通用层。
**理由**：项目核心设计原则；越界会让通用知识被某一分支视角污染。
**校验**：分支层 PR 不得改动 `concepts/` 等通用页的语义。

### P6 单一事实源
**声明**：同一事实只在一处权威定义，其他位置引用而非复制。
**理由**：`cedh-combo-patterns.md` 同文件内 Oracle 胜利条件 :69 正确、:37/:54 错误，正是复制而非引用导致的矛盾。
**校验**：发现重复定义时合并为引用；矛盾即视为缺陷。

---

## 第三编 · 格式规范（Format & Conventions）

### P7 Wiki 页面格式
**声明**：`wiki/` 下页面须含 YAML frontmatter（`created`/`updated`/`type`/`tags`/`sources`）；内部链接用 `[[WikiLink]]`；文件名小写、短横线连接。
**校验**：frontmatter 字段齐全；`type` 取值合法；lint 无断链/孤立页。

### P8 Skill 格式与触发边界
**声明**：`SKILL.md` 须含合法 `name` + `description` frontmatter（缺失会被 opencode 过滤而失效）；`description` 须**同时**声明"触发"与"不触发（边界与让渡对象）"。
**理由**：`modern-breaker` 因缺 frontmatter 实际从未被注册，是"死 skill"；多个 MTG skill 的触发域若不划清会互相抢占。
**校验**：`grep "^name:" / "^description:"` 命中；description 含正反声明。

---

## 第四编 · 治理与变更（Governance & Change）

### P9 提案先行
**声明**：新增/删除/重构 skill、分支结构，或跨页面的内容修正，须**先提交提案（计划 + 架构），经校验通过后再实现**。
**理由**：两次"先斩后奏"（直接建/删 skill）促成本宪法。
**校验**：实质变更存在对应提案与评审记录。

### P10 评审制
**声明**：实质变更须经多视角评审（至少：架构 / 领域正确性 / 集成 / 规范），并记录意见与结论（approve / approve-with-changes / reject）。
**校验**：PR 关联评审记录；未决评审项不得合入。

### P11 可追溯变更与回退
**声明**：变更记入 `wiki/log.md`；破坏性操作须有回退预案；在分支作业、经评审合入；**禁止未提交即删除文件**。
**理由**：曾删除 modern-breaker 后靠 `git checkout` 才得以恢复。
**校验**：log 有对应记录；删除/重构有回退路径。

### P12 宪法自修订
**声明**：本宪法的修改同样须经 P9–P11 流程，并递增版本号、记录变更理由。
**校验**：宪法变更有提案、评审与版本记录。

---

## 贡献者自检清单（提交前逐条勾选）

- [ ] P1 所有引用路径/链接真实存在
- [ ] P2 卡牌/规则/译名经工具或本地原文查证（附输出）
- [ ] P3 数据有来源与时间戳，区分事实与推测
- [ ] P4 无编造内容
- [ ] P5 未越界修改通用知识库
- [ ] P6 无重复定义/内部矛盾
- [ ] P7 wiki 页面 frontmatter + WikiLink + 命名合规
- [ ] P8 skill 有合法 frontmatter 且 description 含正反触发边界
- [ ] P9 实质变更已有提案
- [ ] P11 已在 `wiki/log.md` 记录，破坏性操作有回退预案

## 评审清单（reviewer 使用）

| 视角 | 核查重点 | 对应条款 |
|------|---------|---------|
| 架构 | 分层、单一事实源、定位 | P5, P6 |
| 领域正确性 | 卡牌/规则/数据是否查证属实 | P2, P3, P4 |
| 集成 | 路径真实性、skill 注册与触发边界 | P1, P8 |
| 规范 | frontmatter/命名/链接、治理流程 | P7, P9–P12 |

---

## 待校验事项（本宪法 v0.1 的 open questions）

1. 条款分编是否完备？是否有真实失败模式未被覆盖？
2. 每条「校验判据」是否真正可执行（最好能脚本化，如扩展 `lint_wiki_v2.py`）？
3. 评审"批准人/法定人数/平票处理"尚未定义——是否纳入 P10？
4. `wiki/log.md` 的 skill/governance 操作类型格式待定义。
5. 宪法放置位置（根级 `CONSTITUTION.md`）与中英文命名是否合适？
